name: Docker

on:
  push:
    branches:
      - docker**
      - ghactions
  # https://help.github.com/en/actions/automating-your-workflow-with-github-actions/workflow-syntax-for-github-actions#onschedule
  schedule:
    - cron:  '0 7 * * 0-4' # every work night (delayed 1 hr)
  repository_dispatch:
    types:
      - all
      - docker


jobs:
  docker:
    name: ${{ matrix.docker.type }}-${{ matrix.r_alias }}${{ matrix.flavor.extra_base_tag }}
    runs-on: ubuntu-latest

    strategy:
      max-parallel: 2
      fail-fast: false
      matrix:

        flavor:
          - shinycoreci_sha: master
            extra_base_tag: ""
          # - shinycoreci_sha: shiny-1.4.0.1
          #   extra_base_tag: "-rc_v1.4.0.1"

        r_alias:
          - "release"
          - "oldrel/1"
          - "oldrel/2"
          - "oldrel/3"
          - "oldrel/4"

        docker:
          - name: Centos7
            workdir: Docker/centos
            type: centos7
          - name: Focal
            workdir: Docker/ubuntu
            type: focal
          - name: Bionic
            workdir: Docker/ubuntu
            type: bionic
          - name: Xenial
            workdir: Docker/ubuntu
            type: xenial
            # Xenial has trouble installing R 3.5

    steps:
    - uses: actions/checkout@master

    - name: Get R version
      uses: r-lib/actions/setup-r@master
      id: install-r
      with:
        r-version: ${{ matrix.r_alias }}
        install-r: false # No need to install. Just need version

    - name: Get apps sha
      if: ${{ steps.install-r.outputs.installed-r-version != '3.5' || matrix.docker.type != 'bionic' }}
      shell: bash
      id: apps
      run: |
        SHA=${{ github.event.pull_request.head.sha }}
        if [[ -z "$SHA" ]]; then
          SHA=${{ github.sha }}
        fi
        echo $SHA
        SHORT_SHA=${SHA:0:7}
        echo $SHORT_SHA
        echo "::set-output name=sha::$SHORT_SHA"


    # https://github.com/marketplace/actions/publish-docker
    - name: Build Base
      if: ${{ steps.install-r.outputs.installed-r-version != '3.5' || matrix.docker.type != 'bionic' }}
      uses: elgohr/Publish-Docker-Github-Action@2.12
      with:
        name: rstudio/shinycoreci
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
        cache: ${{ github.event_name == 'push' }}
        workdir: ${{ matrix.docker.workdir }}
        buildargs: "R_VERSION=${{ steps.install-r.outputs.installed-r-version }},RELEASE=${{ matrix.docker.type }},APPS_SHA=${{ steps.apps.outputs.sha }},SHINYCORECI_SHA=${{ matrix.flavor.shinycoreci_sha }},GITHUB_PAT=${{ secrets.GITHUB_PAT }}"
        tags: "base-${{ steps.install-r.outputs.installed-r-version }}-${{ matrix.docker.type }}${{ matrix.flavor.extra_base_tag }}"


    # https://github.com/marketplace/actions/publish-docker
    - name: Build SSO
      if: ${{ steps.install-r.outputs.installed-r-version != '3.5' || matrix.docker.type != 'bionic' }}
      uses: elgohr/Publish-Docker-Github-Action@2.12
      with:
        name: rstudio/shinycoreci
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
        cache: ${{ github.event_name == 'push' }}
        workdir: ${{ matrix.docker.workdir }}_sso
        buildargs: "R_VERSION=${{ steps.install-r.outputs.installed-r-version }},RELEASE=${{ matrix.docker.type }},EXTRA_BASE_TAG=${{ matrix.flavor.extra_base_tag }}"
        tags: "sso-${{ steps.install-r.outputs.installed-r-version }}-${{ matrix.docker.type }}${{ matrix.flavor.extra_base_tag }}"


        # Note: Store the license as a single line string with new lines as visible '\n' characters.
    - name: Store SSP License
      if: ${{ steps.install-r.outputs.installed-r-version != '3.5' || matrix.docker.type != 'bionic' }}
      shell: bash
      run: |
        printf "%b" "${{ secrets.SSP_LICENSE_FILE }}" > ${{ matrix.docker.workdir }}_ssp/ssp.lic 2>/dev/null
        wc -l ${{ matrix.docker.workdir }}_ssp/ssp.lic
    # https://github.com/marketplace/actions/publish-docker
    - name: Build SSP
      if: ${{ steps.install-r.outputs.installed-r-version != '3.5' || matrix.docker.type != 'bionic' }}
      uses: elgohr/Publish-Docker-Github-Action@2.12
      with:
        name: rstudio/shinycoreci
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
        cache: ${{ github.event_name == 'push' }}
        workdir: ${{ matrix.docker.workdir }}_ssp
        # ignoring build arg *_AWS_BUILD_MACHINE as it's consistent within file
        buildargs: "R_VERSION=${{ steps.install-r.outputs.installed-r-version }},RELEASE=${{ matrix.docker.type }},SSP_LICENSE_FILE=ssp.lic,EXTRA_BASE_TAG=${{ matrix.flavor.extra_base_tag }}"
        tags: "ssp-${{ steps.install-r.outputs.installed-r-version }}-${{ matrix.docker.type }}${{ matrix.flavor.extra_base_tag }}"
    - name: Remove SSP License
      if: always()
      run: |
        rm ${{ matrix.docker.workdir }}_ssp/ssp.lic
